import React from 'react';

function ShowModal(props) {
  return (
      <div>
        <button onClick={
          ()=>{
            
          }
        }>Show Modal</button>

      </div>
  );
}
export default ShowModal;