import React from 'react';


function SpecialTextBox(props) {
  return (
      <div>
        Enter Special Text:
        <input onChange={(e)=>{
            
        }} />
      </div>
  );
}

export default (SpecialTextBox);
